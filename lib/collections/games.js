// Defines the Games collection.
// This is pretty much a static collection, so there's not much else.
// See /private/game_fixtures.json to see the default data that will be loaded.

Games = new Mongo.Collection("games");

Meteor.methods({
});
